# ─── 實際資產資料，一律不進版控 ──────────────────────────────────
#
# 測試用的 fixture 是從真實試算表擷取的：持股、股數、成本、各帳戶餘額、
# 950 天淨值曲線。這些可以推上 GAS（那是自己的專案），但**不可以進 GitHub**。
#
# 測試檔本身要進版控，所以它們一律從這裡的 *.data 讀資料，
# 不可以把數字內嵌回 .cjs。重新產生的方式見 test_*.cjs 開頭的說明。
*.data

# clasp 的本機設定（含 scriptId 以外的個人路徑）
.clasprc.json
node_modules/
