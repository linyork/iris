# 進入點資料流盤點 — 死碼、過期註解、冗餘

> 盤點日：2026-08-08，基準 commit `2a6b7d0`。
> 範圍：以 `doPost` / `doGet` 為起點，沿商業邏輯資料流走一遍，只找**不必要的、過期的、
> 以前留下來的**東西 —— 不是功能檢查，也不是找 bug。
>
> 與其他文件的分工：`CLAUDE.md` / `README.md` 講「現在是什麼樣子」，
> `AGENT_REVIEW.md` 講「為什麼要改、往哪裡改」，這份講「哪些東西已經不該還在這裡」。

---

## 盤點基準：兩個進入點的資料流

```
doPost → Line.isLine / Telegram.isTelegram → 去重(CacheService, 6h)
       → checkMaster → indicateTyping
       ├─ document → AssetImport.fromUpload → AssetSchema / Position
       ├─ /xxx     → Commands.tryHandle → Position.rebuild / buildDailyReport / Telegram
       └─ text     → ChatBot.reply → AIServiceFactory → Tools.execute
                                     → AssetTools / GoogleSheet / Snapshot / StockPrice / WebSearch
       → MessagingServiceFactory.push

doGet ─ ?view=tg → MiniAppPage.html → miniAppData / miniAppAsk → MiniApp.verifyInitData
      │                                                        → Dashboard.getPayload
      └─ 其他     → Dashboard.isAuthorized → DashboardPage.html → dashboardData
                                                                → Dashboard.getPayload
                                                                  └→ Snapshot._totals / _metrics
                                                                     / _holdings / _cash / ...
```

主幹是乾淨的，沒有走不到的分支、沒有繞路。以下是掉在路邊的東西。

---

## 第一批：死碼 ✅ 已清除

### 1. `Line.verifySignature` — 定義了，從來沒有人呼叫

不只是沒被呼叫，是**根本不能用**：它讀 `e.parameter['X-Line-Signature']`，但 GAS 的
`doPost(e)` 讀不到 HTTP header —— 這條限制 `Telegram.gs` 的檔頭註解自己就寫著。
同一個專案裡一個檔案寫著限制，另一個檔案留著基於相反假設寫的驗簽函式。

連帶：`LINE_CHANNEL_SECRET` 這個 Script Property 因此沒有任何實際讀取端，
但 `CLAUDE.md` / `README.md` 都還把它的用途寫成「LINE webhook 簽章驗證」。

**處置**：刪掉 `verifySignature`、`Config.LINE_CHANNEL_SECRET` getter 與 `ENV_KEYS.LINE_SECRET`、
`DevTools.setup()` 的那行輸出，兩份文件的設定表移除該列。
GAS 上的 Script Property 本身沒有動，日後真要做驗簽再加回來。

### 2. `Line.getProfile` → `event.profile` 全專案沒有讀取端

`Line.init` 每收到一則 LINE 訊息就打一次 `UrlFetchApp` 拿 profile，丟進一個沒人看的欄位。
doPost 熱路徑上一次純浪費的網路往返，而這個專案的時間預算很緊。

**處置**：刪掉 `getProfile` 與 `event.profile` 的指派。
`getSourceId` / `event.sourceId` **保留** —— 它是中立事件形狀的一部分（Telegram 與 MiniApp
都會設），雖然 LINE 這一側目前沒有讀取端，但它不花任何成本，拿掉會讓事件形狀不一致。

---

## 第二批：過期的註解與文件 ✅ 已更新

| # | 位置 | 問題 |
|---|---|---|
| 3 | `GoogleSheet.getHoldings` | 一個函式上面疊了兩份 JSDoc，第一份描述的是凍結的「所有股票」寬表（`row2 = 0000 合計列`）。改寫時新增了註解卻沒刪舊的。 |
| 4 | `GoogleSheet.gs` 檔頭 | 寫著「舊表的股利鏡像仍在 SHEET_ID」，但 `getDividendHistory` 早就走 `Snapshot._open()` + `AssetSchema.readTrades` 讀「交易」表了。 |
| 5 | `Snapshot.gs` 檔頭 | `@description` 仍寫「顧問感知層的備料模組…給 AdvisorCheck」，實際上 Dashboard / MiniApp / `GoogleSheet` 三個工具全都吃它。另有一整張「舊表 → 新表對應」的遷移期對照表，以及 `_holdings` 裡「舊表推導不出來」的註解。 |
| 6 | `ChatBot.gs` XML 清理 | 註解點名「GLM-5.1」，那顆模型早已不在配置裡（現役是 deepseek-v4-flash + gpt-oss-20b）。防護留著沒問題，但名字會給後人錯誤線索。 |
| 7 | `Main.doGet` 檔頭 | 寫「請另外建立一個『存取權：只有我自己』的 deployment」，實際做法是用 HEAD 的 `/dev`（`Config.DASHBOARD_URL` 與 `CLAUDE.md` 寫的都是這個）。照舊註解做會多建一個沒必要的部署。 |
| 8 | `Prompt.gs` ×2 | 「主動推送 **LINE** 通知」。推播早就走 `MessagingServiceFactory`（實際是 Telegram）。這是餵進 LLM 的提示詞，不只是註解 —— 模型會照這個字眼跟主人講話。 |
| 9 | `Telegram.setupWebhook` | 註解說「過濾 edited_message」，但 `isTelegram` / `normalizeEvent` 都還在處理它。以現行 `allowed_updates: ['message']` 來說那條分支進不去。程式保留當防禦，改註解說明現況。 |
| 10 | `CLAUDE.md` / `README.md` | 斜線指令表漏了 `/refresh` —— 它在 `Commands.gs` 有實作、`Prompt.gs` 還引導使用者去用、`setMyCommands` 也會註冊。（工具數量「21 tools」那句是對的，數過。） |
| 11 | `README.md` 模組表 | `Line.gs` 那列寫「LINE 簽章驗證」，隨第一批一起改成「LINE 事件正規化」。 |

**驗證**：`node test_asset.cjs` → 314/314 全過；九個改動過的 `.gs` 都通過語法檢查。
兩批合計 `+38 / −69` 行。沒有動任何行為，`/refresh` 也沒有新增或改名，
所以**不需要**跑 `setupTelegramCommands()`。

---

## 第三批：冗餘 —— #11 #12 已修，#13 收回

### 11. `ChatBot.reply` 自己開了第二個碼錶 ✅ 已修

```js
var startTime = new Date().getTime();
var NEW_TURN_DEADLINE_MS  = 200000;
var TAIL_CALL_DEADLINE_MS = 280000;
```

而 `Utils.EXEC_START` / `execTimeLeftMs()` 存在的理由，它自己的註解寫得很明確：
「不必由每個進入點自己傳一個 startTime 下來」。`NvidiaService` 與 `AIServiceFactory`
都用 `Utils.execTimeLeftMs()`，只有 ChatBot 用自己的。

差別是實質的：`Utils` 從**檔案載入**（＝本次執行起點）起算，ChatBot 從**進入 reply()**
起算。走 doPost 時差幾百毫秒無所謂，但 `miniAppAsk` 是驗簽 + 一次 push 之後才進 ChatBot，
ChatBot 的錶會少算掉那一段。同一支執行裡有兩個對「還剩多少時間」意見不同的來源，
正是 `Utils` 那段註解想消滅的東西。

### 12. `Config.MODEL_CAPABILITIES` 實質上是死的 ✅ 已刪

唯一讀者是 `GeminiService` 的 `options.maxOutputTokens || modelCaps.maxOutputTokens`，
但 `AIServiceFactory` 一定會先從 tier 設好 `maxOutputTokens`，那個 fallback 永遠取不到值。
`supportsFunctionCalling` 完全沒有讀者。更麻煩的是它的數字（一律 8192）跟
`GEMINI_MODELS`（2048 / 4096 / 6144）對不上 —— 兩份互相矛盾的設定，其中一份沒人在看。

### 13. Dashboard payload 有沒有消費端的欄位 ❌ 收回，維持現狀

事實部分成立：`Snapshot._metrics` 的 `stockRatio` / `cashRatio` / `physicalRatio`、
`_holdings` 的 `isClosed`，兩個頁面都沒有讀（MarketAlert 用的是 `StockPrice` 自己那個
`isClosed`）。

但我原本拿「90KB 單鍵快取上限」當理由，那個理由不成立 —— 這四個純量加起來大概一百個
位元組，同一份 payload 裡躺著 365 個點的 `series`，量級差了三個數量級。

剩下的理由只有「介面該不該只放有人用的東西」，而反方更強：`_metrics` 是「指標」那張表的
**完整投影**，下一個想在面板上加「股票佔比」的人可以直接拿。CLAUDE.md 也寫明新增一列
指標 有三個讀者要改，保持完整投影反而讓那條規則好守。**維持現狀。**

---

## 第四批：假宣稱攔截器的地基 ✅ 已修

原本的判斷是：

```js
if (Tools.isWrite(name)) wroteViaTool = true;   // ← 在 Tools.execute() 之前
```

旗子掀在**呼叫之前**，所以只要模型叫了寫入類工具就算數 —— 工具被業務規則擋下
（賣超）、參數不齊、帳戶不存在、丟例外，帳本一個字沒改，攔截器卻已放行。
偏偏那正是最容易出現假「已記錄」的場合：模型剛被拒絕，手上還有一句確認要交代。

⚠️ 澄清一個容易誤解的地方：這個旗子**不會對 LLM 說任何話**。LLM 看到的是工具的
真實回傳（「擋下不寫」），它拿到的資訊一直是對的。旗子的唯一作用是決定要不要
攔它 —— 所以問題不是「模型被騙」，是**安全網被關掉**。

**做法**：證據改成取自寫入本身。真的動到試算表的六處各叫一次 `Utils.noteLedgerWrite()`，
`ChatBot.reply` 進迴圈前記下 `Utils.ledgerWriteCount()` 當基準，收尾時比對。

考慮過但否決的作法：比對工具回傳的成功字串（「已記錄第 N 列」）。那句話同時是要給
主人看的文案，把安全檢查綁在使用者可見的措辭上，等於哪天有人改了文案、防線就靜靜失效。

⚠️ **不可以改成攔截所有試算表寫入**：`Logger` 每則訊息都寫 consolelog、`ChatBot` 每次
回覆都寫兩列 chat，全域計數會恆為真，等於把防線關掉又看起來像修好了。

**附帶收穫**：`WRITE_TOOLS` 整份刪掉了。它是繼 `definitions` 與 `execute()` 之後
第三個必須同步的地方，而 CLAUDE.md 特別警告漏改不會報錯。改成計數器之後，
工具只要真的寫進去就一定被算到，不必有人記得來登記 —— 要改的地方回到兩處。

**失敗方向是刻意選的**：漏加呼叫點 → 寫成功了卻被加警語（誤報，帳本是對的）。
誤報看得見、會被抱怨；漏報看不見。

`T27` 釘死兩個方向：擋下的賣超不能讓計數器動，寫成功則一定要動 —— 少了後半，
前面三條斷言在一個「永遠不動」的計數器上也會通過。
